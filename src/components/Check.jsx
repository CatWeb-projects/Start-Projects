import React from 'react';

class Check extends React.Component {
  render() {
    return <div></div>;
  }
}

this.hadleClickThis = (e) => {
  [e.target.name] = e.target.value;
};

export default Check;
